diff --git a/.github/workflows/azure-static-web-apps-agreeable-pond-0087d2c00.yml b/.github/workflows/azure-static-web-apps-agreeable-pond-0087d2c00.yml
index cc519b2..af87ce8 100644
--- a/.github/workflows/azure-static-web-apps-agreeable-pond-0087d2c00.yml
+++ b/.github/workflows/azure-static-web-apps-agreeable-pond-0087d2c00.yml
@@ -4,30 +4,35 @@ on:
   workflow_dispatch:
 
 jobs:
-  build_and_deploy_job:
+  build_job:
     runs-on: ubuntu-latest
-    name: Build and Deploy Job
+    name: Build Job
     environment: Default
     permissions:
-       id-token: write
-       contents: read
+      contents: read
+      packages: read
     steps:
       - uses: actions/checkout@v3
         with:
           submodules: true
           lfs: false
-      - name: Install OIDC Client from Core Package
-        run: npm install @actions/core@1.6.0 @actions/http-client
-           
+      - name: Setup Node.js
+        uses: actions/setup-node@v4
+        with:
+          node-version: '22'
+          registry-url: 'https://npm.pkg.github.com'
+          scope: '@adolf94'
+          
       - name: Install Dependencies
         run: npm install --legacy-peer-deps
+        env:
+          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         working-directory: frontend
           
       - name: Build React
         run: npm run build
         working-directory: frontend
   
- 
       - name: Substitute JSON 
         uses: restackio/update-json-file-action@2.1
         with:
@@ -44,36 +49,42 @@ jobs:
               "guarantorRole": "api://${{vars.API_CLIENT_ID}}/guarantor"
             }
 
-      - name: Move/Rename config.json to js
+      - name: Build And Deploy Preparation
         run: |
-              echo window.webConfig= > config.js
-              cat config.json >> config.js
-        working-directory: frontend/dist
+              cp public/staticwebapp.config.json dist/
+              echo window.webConfig= > dist/config.js
+              cat dist/config.json >> dist/config.js
+        working-directory: frontend
         
-      - name: Get Id Token
-        uses: actions/github-script@v6
-        id: idtoken
+      - name: Upload Build Artifact
+        uses: actions/upload-artifact@v4
+        with:
+          name: frontend-dist
+          path: frontend/dist/
+
+  deploy_job:
+    needs: build_job
+    runs-on: ubuntu-latest
+    name: Deploy Job
+    steps:
+      - name: Download Build Artifact
+        uses: actions/download-artifact@v4
         with:
-           script: |
-               const coredemo = require('@actions/core')
-               return await coredemo.getIDToken()
-           result-encoding: string
+          name: frontend-dist
+          path: ./dist
 
       - name: Build And Deploy
         id: builddeploy
         uses: Azure/static-web-apps-deploy@v1
         with:
           azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AGREEABLE_POND_0087D2C00 }}
+          repo_token: ${{ secrets.GITHUB_TOKEN }}
           action: "upload"
-          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
-          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
-          app_location: "./frontend/dist" # App source code path
-          api_location: "" # Api source code path - optional
-          skip_app_build : true,
-          skip_api_build : true,
-          output_location: "dist" # Built app content directory - optional
-          github_id_token: ${{ steps.idtoken.outputs.result }}
-          ###### End of Repository/Build Configurations ######
+          app_location: "./dist"
+          api_location: ""
+          skip_app_build: true
+          skip_api_build: true
+          output_location: ""
 
   close_pull_request_job:
     if: github.event_name == 'pull_request' && github.event.action == 'closed'
@@ -84,4 +95,5 @@ jobs:
         id: closepullrequest
         uses: Azure/static-web-apps-deploy@v1
         with:
+          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AGREEABLE_POND_0087D2C00 }}
           action: "close"
